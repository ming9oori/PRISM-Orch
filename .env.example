# Orch 서버 바인딩
APP_HOST=0.0.0.0
APP_PORT=8100
RELOAD=true

# vLLM(OpenAI-Compat) 서버 주소
# - 로컬 vLLM(코어 docker-compose)이면:
OPENAI_BASE_URL=http://localhost:8001/v1
# - 원격 vLLM이면(예: 사내 vLLM 게이트웨이):
# OPENAI_BASE_URL=http://your-remote-vllm:8001/v1
OPENAI_API_KEY=EMPTY

# PRISM-Core API 주소 (Orch → Core 호출용)
PRISM_CORE_BASE_URL=http://localhost:8000